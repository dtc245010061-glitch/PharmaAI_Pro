# ==========================================
# 1. CORE FRAMEWORK (Máy chủ & Giao tiếp)
# ==========================================
fastapi>=0.110.0
uvicorn[standard]>=0.27.1
python-multipart>=0.0.9

# ==========================================
# 2. CONFIGURATION & VALIDATION (Cấu hình & Bắt lỗi)
# ==========================================
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# ==========================================
# 3. DATABASE & ORM (Quản lý Cơ sở dữ liệu)
# ==========================================
sqlalchemy>=2.0.27
alembic>=1.13.1

# Database Drivers (Trình điều khiển kết nối DB)
pymysql>=1.1.0          # Dành cho MySQL
cryptography>=42.0.5    # Bổ trợ cho MySQL và JWT
psycopg2-binary>=2.9.9  # Dành cho PostgreSQL

# ==========================================
# 4. SECURITY & AUTHENTICATION (Bảo mật & Phân quyền)
# ==========================================
passlib[bcrypt]>=1.7.4          # Thuật toán băm mật khẩu
python-jose[cryptography]>=3.3.0  # Tạo và giải mã JWT Token

# ==========================================
# 5. AI INTEGRATION (Tích hợp Trí tuệ nhân tạo)
# ==========================================
google-generativeai>=0.4.1      # SDK kết nối Google Gemini AI
openai>=1.13.3                  # SDK kết nối OpenAI (ChatGPT)

# ==========================================
# 6. UTILS (Các công cụ hỗ trợ khác)
# ==========================================
httpx>=0.27.0                   # Gọi API bất đồng bộ